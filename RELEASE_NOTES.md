# 磁盘观察室 v0.7.0

## 下载

- `DiskGuard-Desktop.exe`：Windows 10/11 x64 便携程序。
- `DiskGuard-Desktop-portable.zip`：同版 EXE、说明和 MIT 许可证。
- 无需安装 Python；使用 Windows 自带的 .NET Framework/WPF。不是驱动安装器，不自动执行磁盘写入测试。

## 本版更新

- 悬浮窗按“原始数据 / 计算指标 / 结论”分类，所有列出的指标可以单独勾选；节点 IP、位置、风控等放入独立附加功能区。
- 默认显示 9 项：读速、写速、温度、磨损、不安全关机、健康结论、近期写入状态、节点 IP、节点位置。已有配置保留，可通过“恢复推荐指标”采用新默认值。
- 主界面与悬浮窗共用指标目录；类别可以折叠，刷新不重置折叠状态。
- 明确“无法获取 / 样本不足 / 阈值未知 / 不适用 / 数据过期”；采集失败不补零，不把未见告警解释成完全健康。
- 忙碌占比改为 100% − 系统空闲占比；删除统一的摄氏温度 90% 预警，磨损达到 100% 不再直接判定设备已损坏。
- 改善字体、对齐、窄窗口布局、深浅主题和下拉交互；保留可视化流程图和悬停帮助。
- 默认兼容软件渲染；关闭窗口隐藏到托盘，隐藏界面暂停图表重绘。保持单实例。
- 每块物理硬盘可独立选择探针目录和参与状态，支持浏览选目录。探针默认关闭，启用会产生真实读写。

## 评价边界

性能瓶颈缺少区间延迟及同类负载参照时不作判断；长期退化缺少跨重启快照时显示样本不足。现有学习仅在内存中，不输出剩余寿命或故障概率。Ping0 查询需单独启用；风控值需要服务方 API Key，可能产生费用，Key 不随发布分发。

## 验证

通过模型边界、指标分类、默认配置、独立勾选、配置序列化、折叠状态、深浅主题、下拉交互、托盘行为及探针安全回归测试。不同设备的字段可用性与屏幕观感仍取决于硬件、驱动和显示环境。
